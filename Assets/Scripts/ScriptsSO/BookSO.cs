using System.Collections;
using System.Collections.Generic;
using UnityEngine;
[CreateAssetMenu(fileName = "BookS0", menuName = "BookSO")]

public class BookSO : MonoBehaviour
{
    public List<TextAsset> PagesText;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
